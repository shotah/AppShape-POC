on:
  issues:
    types: [labeled]

env:
  DISCORD_WEBHOOK: ${{ secrets.DISCORD_WEBHOOK }}

jobs:
  poll:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - name: poll
        # if: contains(github.event.label, 'enhancement')
        uses: Ilshidur/action-discord@master
        with:
          args: "Test ${{ github.event.issues.*.labels.*.name }}"

